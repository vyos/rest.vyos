---
- debug:
    msg: START vyos_ha deleted integration tests on connection={{ ansible_connection }}

- include_tasks: _remove_config.yaml
- block:
    - include_tasks: _populate_config.yaml

    - name: Delete all HA configuration
      vyos.rest.vyos_ha: &id001
        state: deleted
      register: result

    - assert:
        that:
          - result.changed == true

    - name: Gather after delete
      vyos.rest.vyos_ha:
        state: gathered
      register: gathered

    - assert:
        that:
          - gathered.gathered == {}

    - name: Delete again (IDEMPOTENT)
      vyos.rest.vyos_ha: *id001
      register: result

    - assert:
        that:
          - result.changed == false

  always:
    - include_tasks: _remove_config.yaml
