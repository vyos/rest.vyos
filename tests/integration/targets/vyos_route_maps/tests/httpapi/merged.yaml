---
- debug:
    msg: START vyos_route_maps merged integration tests on connection={{ ansible_connection }}

- include_tasks: _remove_config.yaml

- block:
    - name: Merge route_maps configuration
      register: result
      vyos.rest.vyos_route_maps: &id001
        config:
          - route_map: RM-TEST-EXPORT-POLICY
            entries:
              - sequence: 10
                action: permit
                match:
                  peer: 192.0.2.32
                set:
                  metric: "5"
                  as_path_exclude: "111"
                  aggregator:
                    as: 100
        state: merged

    - assert:
        that:
          - result.changed == true

    - name: Merge route_maps configuration (IDEMPOTENT)
      register: result
      vyos.rest.vyos_route_maps: *id001

    - name: Assert idempotent
      assert:
        that:
          - result.changed == false
          - result.commands == []

  always:
    - include_tasks: _remove_config.yaml
