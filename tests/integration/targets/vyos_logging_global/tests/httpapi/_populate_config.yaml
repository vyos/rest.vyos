---
- name: Populate logging_global config for testing
  vyos.rest.vyos_logging_global:
    config:
      console:
        facilities:
          - facility: local7
            severity: err
      global_params:
        facilities:
          - facility: cron
            severity: debug
        preserve_fqdn: true
    state: merged
  ignore_errors: true
